services:
  go-streamer:
    image: go-streamer:local
    build: .
    container_name: go-streamer
    restart: unless-stopped
    ports:
      - '3000:3000'
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    devices:
      - /dev/dri:/dev/dri
